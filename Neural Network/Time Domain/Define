% Define dataset parameters
numSamples = 1000; % Number of samples
numFeatures = 20;  % Number of Time Domain features
numClasses = 5;    % Number of classes (users)

% Generate random feature data
X = rand(numSamples, numFeatures); % Random features (1000 samples x 20 features)

% Generate random class labels for the samples
Y = randi([1, numClasses], numSamples, 1); % Labels between 1 and 5 (1000 samples)
Y = categorical(Y); % Convert labels to categorical for classification

% Save the mock dataset to a .mat file
save('TimeD_Data.mat', 'X', 'Y'); % Save the dataset to a file
disp('Mock dataset created and saved as TimeD_Data.mat.');
